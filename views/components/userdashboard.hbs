<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        /* General Body & Container */
body.dashboard-body {
    font-family: 'Inter', 'Segoe UI', Roboto, Helvetica, Arial, sans-serif, 'Apple Color Emoji', 'Segoe UI Emoji', 'Segoe UI Symbol';
    margin: 0;
    padding: 0;
    background: linear-gradient(135deg, #f5f7fa, #c3cfe2); /* Soft blue-grey gradient */
    color: #333;
    display: flex;
    justify-content: center;
    align-items: flex-start; /* Align to top, allows scroll */
    min-height: 100vh;
    padding-top: 20px; /* Space from top */
    padding-bottom: 20px; /* Space from bottom */
}

.dashboard-container {
    width: 95%;
    max-width: 1300px; /* Wider container */
    background-color: #ffffff;
    border-radius: 15px; /* Softer corners */
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    overflow: hidden;
    display: grid;
    grid-template-rows: auto 1fr auto;
    margin: 0 auto; /* Center the container */
    animation: fadeInScale 0.6s ease-out forwards; /* Initial animation */
}

/* Header */
.dashboard-header {
    background-color: #2c3e50; /* Darker, sophisticated navy */
    color: white;
    padding: 1.2em 2.5em;
    border-bottom: 5px solid #3498db; /* Accent blue */
}

.header-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap; /* Allow wrapping on small screens */
}

.app-title {
    margin: 0;
    font-size: 1.8em;
    font-weight: 600;
}

.app-title .tagline {
    font-size: 0.6em;
    opacity: 0.7;
    margin-left: 0.8em;
    font-weight: 400;
}

/* Navigation */
.main-nav ul.nav-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-wrap: wrap; /* Allow wrapping */
}

.main-nav ul.nav-list li.nav-item {
    margin-left: 1.8em; /* More space between items */
}

.main-nav ul.nav-list li.nav-item a,
.main-nav ul.nav-list li.nav-item button.logout-button {
    color: white;
    text-decoration: none;
    background: none;
    border: none;
    padding: 0.6em 1em;
    border-radius: 8px; /* Rounded buttons */
    cursor: pointer;
    font-size: 0.95em;
    transition: background-color 0.3s ease, transform 0.2s ease;
    display: flex; /* For icon alignment */
    align-items: center;
}

.main-nav ul.nav-list li.nav-item a i,
.main-nav ul.nav-list li.nav-item button.logout-button i {
    margin-right: 0.6em; /* Space for icons */
}

.main-nav ul.nav-list li.nav-item a:hover,
.main-nav ul.nav-list li.nav-item button.logout-button:hover {
    background-color: rgba(255, 255, 255, 0.15);
    transform: translateY(-2px); /* Slight lift on hover */
}

/* Profile link in nav */
.profile-link .profile-thumb {
    width: 30px;
    height: 30px;
    border-radius: 50%;
    object-fit: cover;
    margin-right: 8px;
    border: 2px solid rgba(255, 255, 255, 0.5);
}

.notifications-link {
    position: relative;
}

.notification-badge {
    background-color: #e74c3c; /* Brighter red */
    color: white;
    border-radius: 50%;
    padding: 0.3em 0.6em;
    font-size: 0.7em;
    position: absolute;
    top: -0.8em; /* Adjusted position */
    right: -0.8em;
    min-width: 1.5em; /* Ensures circular shape */
    text-align: center;
    box-shadow: 0 0 5px rgba(0,0,0,0.2);
}


/* Main Content Grid */
.dashboard-main-content {
    padding: 2.5em;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); /* Adjusted min-width for cards */
    gap: 30px; /* Larger gap */
}

/* Card Styling */
.card {
    background-color: white;
    padding: 2em;
    border-radius: 12px;
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.08);
    transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
    border-top: 5px solid transparent; /* Placeholder for individual card border */
}

.card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.12);
}

.card-title {
    color: #333;
    margin-top: 0;
    padding-bottom: 0.8em;
    margin-bottom: 1.5em;
    border-bottom: 1px solid #eee;
    font-size: 1.4em;
    font-weight: 600;
    display: flex;
    align-items: center;
}

.card-title i {
    margin-right: 0.8em;
    color: #3498db; /* Blue accent */
}

/* Welcome Card Specifics */
.welcome-card {
    grid-column: 1 / -1; /* Spans full width */
    border-top-color: #3498db; /* Specific border color */
    display: flex;
    flex-direction: column;
    justify-content: space-between;
}

.welcome-header {
    display: flex;
    align-items: center;
    margin-bottom: 1.5em;
}

.profile-pic {
    width: 80px;
    height: 80px;
    border-radius: 50%;
    object-fit: cover;
    margin-right: 1.5em;
    border: 3px solid #3498db;
    box-shadow: 0 0 0 5px rgba(52, 152, 219, 0.2);
}

.welcome-greeting {
    color: #2c3e50; /* Darker heading */
    font-size: 2.2em;
    margin-bottom: 0.1em;
    font-weight: 700;
}

.tagline-text {
    color: #777;
    font-size: 1.1em;
}

.action-area {
    display: flex;
    gap: 15px;
    flex-wrap: wrap; /* Allow wrapping */
}

/* Buttons */
.primary-button, .secondary-button {
    border: none;
    padding: 1em 1.8em;
    border-radius: 8px;
    cursor: pointer;
    font-size: 1em;
    font-weight: 600;
    transition: background-color 0.3s ease, transform 0.2s ease, box-shadow 0.2s ease;
    display: flex;
    align-items: center;
    justify-content: center;
}

.primary-button i, .secondary-button i {
    margin-right: 0.7em;
}

.primary-button {
    background-color: #27ae60; /* Green for primary actions */
    color: white;
}

.primary-button:hover {
    background-color: #229a56;
    transform: translateY(-2px);
    box-shadow: 0 4px 10px rgba(39, 174, 96, 0.3);
}

.secondary-button {
    background-color: #e0e0e0; /* Lighter grey for secondary */
    color: #555;
    border: 1px solid #ccc;
}

.secondary-button:hover {
    background-color: #d0d0d0;
    transform: translateY(-2px);
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
}

/* Summary Card */
.summary-card {
    border-top-color: #f39c12; /* Orange accent */
}

.summary-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
    gap: 15px;
}

.summary-metric {
    background-color: #fcfcfc;
    border: 1px solid #eee;
    border-radius: 8px;
    padding: 1em;
    text-align: center;
}

.metric-value {
    font-size: 1.8em;
    color: #2c3e50;
    font-weight: 700;
    display: block;
    margin-bottom: 0.1em;
}

.metric-label {
    font-size: 0.9em;
    color: #777;
}

/* Book Sections */
.book-section {
    border-top-color: #9b59b6; /* Purple accent */
}

.book-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(180px, 1fr)); /* Slightly larger book items */
    gap: 20px;
    margin-top: 1.5em;
}

.book-item {
    background-color: #fefefe;
    border: 1px solid #e0e0e0;
    border-radius: 10px; /* Softer corners */
    padding: 1em;
    text-align: center;
    box-shadow: 0 3px 8px rgba(0,0,0,0.05);
    transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
}

.book-item:hover {
    transform: translateY(-4px);
    box-shadow: 0 6px 15px rgba(0,0,0,0.1);
}

.book-item img {
    max-width: 100%;
    height: 180px; /* Fixed height for consistency */
    object-fit: cover; /* Crop to fit */
    border-radius: 6px;
    margin-bottom: 0.8em;
    box-shadow: 0 2px 5px rgba(0,0,0,0.1);
}

.book-item .book-title {
    font-weight: 600;
    font-size: 1.15em;
    color: #333;
    margin-bottom: 0.2em;
    line-height: 1.3;
}

.book-item .book-author {
    font-size: 0.9em;
    color: #666;
    margin-bottom: 0.6em;
}

.book-item .book-price {
    font-size: 1.3em;
    color: #27ae60; /* Green price */
    font-weight: 700;
}

.book-item .book-purchase-date {
    font-size: 0.8em;
    color: #888;
    margin-top: 0.5em;
}


/* Activity Feed */
.activity-card {
    border-top-color: #e67e22; /* Another orange accent */
}

.activity-feed {
    list-style: none;
    padding: 0;
}

.activity-feed li {
    padding: 0.8em 0;
    border-bottom: 1px solid #eee;
    color: #555;
    display: flex;
    align-items: center;
}

.activity-feed li:last-child {
    border-bottom: none;
}

.activity-feed li i {
    margin-right: 0.8em;
    color: #888; /* Icon color */
    font-size: 0.9em;
}

/* Empty States */
.empty-state {
    text-align: center;
    color: #888;
    font-style: italic;
    padding: 2em;
    border: 1px dashed #e0e0e0;
    border-radius: 8px;
    margin: 1em 0;
}

/* Footer */
.dashboard-footer {
    text-align: center;
    padding: 1.5em 2.5em;
    background-color: #2c3e50; /* Matches header */
    color: #ecf0f1; /* Light grey */
    font-size: 0.85em;
    border-top: 1px solid #34495e;
}

/* Animations */
@keyframes fadeInScale {
    from {
        opacity: 0;
        transform: scale(0.95);
    }
    to {
        opacity: 1;
        transform: scale(1);
    }
}

/* Responsive adjustments */
@media (max-width: 992px) {
    .dashboard-main-content {
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 25px;
    }
    .welcome-greeting {
        font-size: 1.8em;
    }
}

@media (max-width: 768px) {
    .header-content {
        flex-direction: column;
        align-items: flex-start;
    }
    .main-nav ul.nav-list {
        flex-direction: column;
        width: 100%;
        margin-top: 1.5em;
    }
    .main-nav ul.nav-list li.nav-item {
        margin: 0.5em 0;
        width: 100%;
    }
    .main-nav ul.nav-list li.nav-item a,
    .main-nav ul.nav-list li.nav-item button {
        justify-content: flex-start;
        width: 100%;
    }
    .profile-pic {
        width: 60px;
        height: 60px;
        margin-bottom: 1em;
    }
    .welcome-header {
        flex-direction: column;
        align-items: flex-start;
    }
    .action-area {
        flex-direction: column;
    }
    .dashboard-container {
        width: 100%;
        margin: 0;
        border-radius: 0;
        box-shadow: none;
    }
    body.dashboard-body {
        padding: 0;
    }
}

@media (max-width: 480px) {
    .dashboard-header, .dashboard-main-content, .dashboard-footer {
        padding-left: 1.5em;
        padding-right: 1.5em;
    }
    .app-title {
        font-size: 1.5em;
    }
    .welcome-greeting {
        font-size: 1.5em;
    }
    .summary-grid, .book-grid {
        grid-template-columns: 1fr;
    }
}
    </style>
</head>
<body>
    <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your BookHub Dashboard - Lusaka</title>
    <link rel="stylesheet" href="dashboard.css">
    <script src="https://kit.fontawesome.com/your_fontawesome_kit.js" crossorigin="anonymous"></script>
</head>
<body class="dashboard-body">
    <div class="dashboard-container">
        <header class="dashboard-header">
            <div class="header-content">
                <h1 class="app-title">BookHub <span class="tagline">Your Literary Exchange</span></h1>
                <nav class="main-nav">
                    <ul class="nav-list">
                        <li class="nav-item profile-link">
                            <a href="#">
                                <img src="https://via.placeholder.com/30" alt="Profile" class="profile-thumb">
                                <span id="user-name-nav"></span>
                            </a>
                        </li>
                        <li class="nav-item notifications-link">
                            <a href="#">
                                <i class="fas fa-bell"></i> Notifications <span id="notification-count" class="notification-badge">0</span>
                            </a>
                        </li>
                        <li class="nav-item"><a href="#"><i class="fas fa-cogs"></i> Settings</a></li>
                        <li class="nav-item"><button id="logout-btn" class="logout-button"><i class="fas fa-sign-out-alt"></i> Logout</button></li>
                    </ul>
                </nav>
            </div>
        </header>

        <main class="dashboard-main-content">
            <section id="welcome-section" class="card welcome-card">
                <div class="welcome-header">
                    <img src="https://via.placeholder.com/80?text=Profile" alt="User Profile Picture" class="profile-pic">
                    <div>
                        <h2 class="welcome-greeting">Jambo, <span id="user-name-main"></span>!</h2>
                        <p class="tagline-text">Ready to dive into new stories or share your own?</p>
                    </div>
                </div>
                <div class="action-area">
                    <button id="create-listing-btn" class="primary-button create-listing-button">
                        <i class="fas fa-plus-circle"></i> List a New Book
                    </button>
                    <button class="secondary-button"><i class="fas fa-search"></i> Discover Books</button>
                </div>
            </section>

            <section id="account-summary" class="card summary-card">
                <h3 class="card-title">Your Hub Snapshot</h3>
                <div class="summary-grid">
                    <div class="summary-metric">
                        <span class="metric-value" id="account-balance">ZMW 0.00</span>
                        <span class="metric-label">Wallet Balance</span>
                    </div>
                    <div class="summary-metric">
                        <span class="metric-value" id="total-books-sold">0</span>
                        <span class="metric-label">Books Sold</span>
                    </div>
                    <div class="summary-metric">
                        <span class="metric-value" id="total-books-bought">0</span>
                        <span class="metric-label">Books Acquired</span>
                    </div>
                </div>
            </section>

            <section id="my-books-for-sale" class="card book-section">
                <h3 class="card-title">Your Listings <i class="fas fa-tag"></i></h3>
                <div id="books-for-sale-list" class="book-grid">
                    <p class="empty-state">You haven't listed any books yet. Click "List a New Book" to get started!</p>
                    </div>
            </section>

            <section id="my-purchases" class="card book-section">
                <h3 class="card-title">Your Collection <i class="fas fa-book-reader"></i></h3>
                <div id="my-purchases-list" class="book-grid">
                    <p class="empty-state">No books in your purchased collection yet. Time to explore!</p>
                    </div>
            </section>

            <section id="recent-activity" class="card activity-card">
                <h3 class="card-title">Recent Activity <i class="fas fa-history"></i></h3>
                <ul id="activity-list" class="activity-feed">
                    <li class="empty-state">Nothing new to report.</li>
                </ul>
            </section>
        </main>

        <footer class="dashboard-footer">
            <p>&copy; 2024 BookHub. Connecting readers in Lusaka and beyond.</p>
        </footer>
    </div>

    <script src="dashboard.js"></script>
</body>
</html>
</body>
</html>