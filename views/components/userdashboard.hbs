<div>
  <div id="card-section">
    <div class="container">
      <div class="card" id="alluserBtn" onclick="showPanel('create')">
        Create</div>
      <div class="card" onclick="showPanel('user-activity')">User Activity</div>
      <div class="card" onclick="showPanel('profile')">Profile</div>
      <div
        class="card"
        onclick="showPanel('subscriptionsPayments')"
      >Subscriptions & Payments</div>
    </div>
  </div>

  <!-- Content Panels -->
  <div id="createDiv" class="content-panel"><button
      class="close-btn"
      onclick="hidePanel()"
    >×</button><h2>Create</h2></div>
  <div id="useractivityDiv" class="content-panel"><button
      class="close-btn"
      onclick="hidePanel()"
    >×</button><h2>User Activity</h2>
  </div>
  <div id="profileDiv" class="content-panel"><button
      class="close-btn"
      onclick="hidePanel()"
    >×</button><h2>Profile</h2></div>
  <div id="subscriptionspaymentsDiv" class="content-panel"><button
      class="close-btn"
      onclick="hidePanel()"
    >×</button><h2>Subscriptions & Payments</h2></div>
</div>